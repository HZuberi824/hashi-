<div id="comment_pane" >
  <%= form_with model: [ @photo, @photo.comments.build ],local: false do |form| %>
    <p>
      <%= form.label :commentor %><br>
      <%= form.text_field :commentor %>
    </p>
    <p>
      <%= form.label :body %><br>
      <%= form.text_area :body %>
    </p>
    <p>
      <%= form.submit %>
    </p>
  <% end %>
</div>